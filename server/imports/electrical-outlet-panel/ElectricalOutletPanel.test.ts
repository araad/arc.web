describe('ElectricalOutletPanel', function () {
    it('throws an exception if it does not derive from PanelInterfaceBase class', function () {
        chai.assert(true);
    });

    it('throws an exception if it does not implement IElectricalOutletPanel interface', function () {
        chai.assert(true);
    });

    it('populates its fields by getting resource values from mbed connector', function () {
        chai.assert(true);
    });

    describe('subscribe (subscribe to resource changes on mbed connector)', function () {
        it('calls the "subscribe" method on each of its modules', function () {
            chai.assert(true);
        });
    });

    describe('unsubscribe (unsubscribe from resource changes on mbed connector)', function () {
        it('calls the "unsubscribe" method on each of its modules', function () {
            chai.assert(true);
        });
    });
});